- hosts: all
  tasks:
    - name: Include OS-specific variables
      include_vars: "{{ ansible_os_family }}.yaml"
    - name: "Stop etcd services"
      service:
        name: "{{ item }}"
        state: stopped
        enabled: no
      become: yes
      loop: "{{ etcd_services }}"
